name: Browser Smoke Test

on:
  workflow_dispatch:

jobs:
  browser:
    runs-on: macos-15
    timeout-minutes: 30

    defaults:
      run:
        working-directory: SocialControl-v0.3-CI/SocialControl-v0.3-CI

    steps:
      - name: Descargar proyecto
        uses: actions/checkout@v4

      - name: Instalar XcodeGen
        run: brew install xcodegen

      - name: Generar proyecto
        run: xcodegen generate

      - name: Compilar
        run: |
          xcodebuild \
            -project SocialControl.xcodeproj \
            -scheme SocialControl \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath DerivedData \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            clean build

      - name: Elegir y arrancar simulador
        run: |
          DEVICE_ID=$(xcrun simctl list devices available \
            | grep -m 1 -E "iPhone" \
            | grep -oE '[0-9A-F-]{36}')

          echo "Simulador elegido: $DEVICE_ID"

          if [ -z "$DEVICE_ID" ]; then
            xcrun simctl list devices available
            exit 1
          fi

          xcrun simctl boot "$DEVICE_ID" || true
          open -a Simulator
          xcrun simctl bootstatus "$DEVICE_ID" -b

      - name: Instalar app
        run: |
          APP_PATH=$(find DerivedData/Build/Products/Debug-iphonesimulator \
            -name "SocialControl.app" \
            | head -1)

          xcrun simctl install booted "$APP_PATH"

      - name: Probar acceso directo a Instagram Reels
        run: |
          xcrun simctl launch \
            booted \
            com.yourcompany.SocialControl \
            -skipOnboarding \
            -debugPlatform instagram \
            -debugURL https://www.instagram.com/reels/

          sleep 12

          xcrun simctl io \
            booted \
            screenshot instagram-direct-block.png

          xcrun simctl terminate \
            booted \
            com.yourcompany.SocialControl || true

      - name: Probar acceso directo a YouTube Shorts
        run: |
          xcrun simctl launch \
            booted \
            com.yourcompany.SocialControl \
            -skipOnboarding \
            -debugPlatform youtube \
            -debugURL https://m.youtube.com/shorts/dQw4w9WgXcQ

          sleep 12

          xcrun simctl io \
            booted \
            screenshot youtube-direct-block.png

      - name: Subir capturas
        uses: actions/upload-artifact@v4
        with:
          name: direct-route-test
          path: |
            SocialControl-v0.3-CI/SocialControl-v0.3-CI/instagram-direct-block.png
            SocialControl-v0.3-CI/SocialControl-v0.3-CI/youtube-direct-block.png
